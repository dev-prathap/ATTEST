FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml README.md ./
COPY attest ./attest
COPY cloud ./cloud
RUN pip install --no-cache-dir . && pip install --no-cache-dir "./cloud[postgres]"
EXPOSE 8400
CMD ["uvicorn", "attest_cloud.main:app", "--host", "0.0.0.0", "--port", "8400"]

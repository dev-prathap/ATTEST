"""Framework adapters (doc 03 §2, entry point 2). Each wraps every tool of a framework with Attest so
one line covers the whole agent. Import the framework you use: `from attest.adapters import langgraph`."""
